{
  "name": "java-memory-safety-docs",
  "private": true,
  "version": "1.0.0",
  "description": "Slides for the Java Memory Safety presentation.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf dist && cp -R node_modules/reveal.js/dist/. dist/ && find dist -name '*.d.ts' -delete",
    "test": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.0",
    "reveal.js": "6.0.1"
  }
}
